# TruLaL
Aggregates the non‑replicated lastLogon attribute from all domain controllers to produce an accurate, audit‑ready last logon timestamp. Includes optional CSV export, per‑DC detail, and comparison with replicated LastLogonDate.
